/**
 * Set to true while the main app is still under development.
 * Displays a banner at the top of the landing page.
 * Flip to false to remove the banner before going live.
 */
export const IN_DEVELOPMENT = true;
